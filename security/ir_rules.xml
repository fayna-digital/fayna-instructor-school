<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Portal users see only their own enrollments -->
        <record id="rule_enrollment_portal_own" model="ir.rule">
            <field name="name">Enrollment: portal sees own only</field>
            <field name="model_id" ref="model_fayna_instructor_enrollment"/>
            <field name="domain_force">[('partner_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Internal users see all enrollments -->
        <record id="rule_enrollment_internal_all" model="ir.rule">
            <field name="name">Enrollment: internal sees all</field>
            <field name="model_id" ref="model_fayna_instructor_enrollment"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
