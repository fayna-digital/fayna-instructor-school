<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================================ -->
    <!--  Kanban view                                                  -->
    <!-- ============================================================ -->
    <record id="fayna_instructor_course_kanban" model="ir.ui.view">
        <field name="name">instructor.course.kanban</field>
        <field name="model">instructor.course</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column">
                <field name="name"/>
                <field name="state"/>
                <field name="start_date"/>
                <field name="enrolled_count"/>
                <field name="available_spots"/>
                <field name="price"/>
                <field name="currency_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <strong><field name="name"/></strong>
                                <div class="text-muted">
                                    <field name="start_date"/>
                                    <t t-if="record.location.raw_value">
                                        — <field name="location"/>
                                    </t>
                                </div>
                                <div>
                                    <field name="price" widget="monetary"/>
                                </div>
                                <div class="mt-1">
                                    <span class="badge bg-info">
                                        <field name="enrolled_count"/> enrolled
                                    </span>
                                    <span class="badge bg-success ms-1">
                                        <field name="available_spots"/> spots left
                                    </span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ============================================================ -->
    <!--  Action (kanban entry point)                                  -->
    <!-- ============================================================ -->
    <record id="action_fayna_instructor_course" model="ir.actions.act_window">
        <field name="name">Courses</field>
        <field name="res_model">instructor.course</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="search_view_id" ref="instructor_course_search_view"/>
        <field name="context">{'search_default_open': 1}</field>
    </record>
</odoo>
