<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================================ -->
    <!--  Tree view                                                    -->
    <!-- ============================================================ -->
    <record id="instructor_course_list" model="ir.ui.view">
        <field name="name">instructor.course.tree</field>
        <field name="model">instructor.course</field>
        <field name="arch" type="xml">
            <tree decoration-muted="state == 'cancelled'"
                  decoration-success="state == 'open'"
                  decoration-info="state == 'in_progress'">
                <field name="name"/>
                <field name="course_type"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="location" optional="show"/>
                <field name="duration_hours" optional="hide"/>
                <field name="max_participants"/>
                <field name="enrolled_count"/>
                <field name="available_spots"/>
                <field name="price" widget="monetary" optional="show"/>
                <field name="currency_id" optional="hide"/>
                <field name="website_published" optional="show"/>
                <field name="state"
                       widget="badge"
                       decoration-success="state == 'open'"
                       decoration-info="state == 'in_progress'"
                       decoration-primary="state == 'completed'"
                       decoration-muted="state in ('draft', 'cancelled')"/>
            </tree>
        </field>
    </record>

    <!-- ============================================================ -->
    <!--  Form view                                                    -->
    <!-- ============================================================ -->
    <record id="instructor_course_form_view" model="ir.ui.view">
        <field name="name">instructor.course.form</field>
        <field name="model">instructor.course</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_open_enrollment" type="object"
                            string="Open Enrollment"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_start" type="object"
                            string="Start Course"
                            class="btn-primary"
                            invisible="state != 'open'"/>
                    <button name="action_complete" type="object"
                            string="Complete"
                            class="btn-success"
                            invisible="state != 'in_progress'"/>
                    <button name="action_cancel" type="object"
                            string="Cancel"
                            class="btn-danger"
                            invisible="state in ('completed', 'cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,open,in_progress,completed"/>
                </header>
                <sheet>
                    <!-- Smart button: enrollment count -->
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_enrollments"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-users">
                            <field name="enrollment_count" widget="statinfo"
                                   string="Enrollments"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Course name…"/></h1>
                    </div>
                    <group>
                        <group string="Course Details">
                            <field name="course_type"/>
                            <field name="duration_hours"/>
                            <field name="website_published"/>
                        </group>
                        <group string="Schedule &amp; Location">
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="location"/>
                        </group>
                        <group string="Capacity &amp; Pricing">
                            <field name="max_participants"/>
                            <field name="enrolled_count"/>
                            <field name="available_spots"/>
                            <field name="currency_id" options="{'no_create': True}"/>
                            <field name="price" widget="monetary"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" widget="html"/>
                        </page>
                        <page string="Enrollments">
                            <field name="enrollment_ids" context="{'default_course_id': active_id}">
                                <tree editable="bottom"
                                      decoration-muted="state == 'cancelled'"
                                      decoration-success="state == 'confirmed'">
                                    <field name="partner_id"/>
                                    <field name="enrollment_date"/>
                                    <field name="payment_status" widget="badge"
                                           decoration-success="payment_status == 'paid'"
                                           decoration-warning="payment_status == 'unpaid'"/>
                                    <field name="state" widget="badge"
                                           decoration-success="state == 'confirmed'"
                                           decoration-info="state == 'pending'"
                                           decoration-muted="state == 'cancelled'"
                                           decoration-primary="state == 'completed'"/>
                                    <button name="action_confirm" type="object"
                                            string="Confirm" icon="fa-check"
                                            invisible="state != 'pending'"/>
                                    <button name="action_cancel" type="object"
                                            string="Cancel" icon="fa-times"
                                            invisible="state in ('cancelled', 'completed')"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ============================================================ -->
    <!--  Search view                                                  -->
    <!-- ============================================================ -->
    <record id="instructor_course_search_view" model="ir.ui.view">
        <field name="name">instructor.course.search</field>
        <field name="model">instructor.course</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="location"/>
                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Open" name="open" domain="[('state','=','open')]"/>
                <filter string="In Progress" name="in_progress" domain="[('state','=','in_progress')]"/>
                <filter string="Completed" name="completed" domain="[('state','=','completed')]"/>
                <filter string="Cancelled" name="cancelled" domain="[('state','=','cancelled')]"/>
                <separator/>
                <filter string="Has Spots" name="has_spots" domain="[('available_spots','>',0)]"/>
                <filter string="Published" name="published" domain="[('website_published','=',True)]"/>
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                    <filter string="Course Type" name="group_type" context="{'group_by': 'course_type'}"/>
                    <filter string="Start Date" name="group_start" context="{'group_by': 'start_date:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ============================================================ -->
    <!--  Action                                                       -->
    <!-- ============================================================ -->
    <record id="action_instructor_course" model="ir.actions.act_window">
        <field name="name">Courses</field>
        <field name="res_model">instructor.course</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="instructor_course_search_view"/>
    </record>
</odoo>
